
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-mfe
  namespace: mfe-demo
  annotations:
    cert-manager.io/issuer: letsencrypt
spec:
  ingressClassName: nginx
  rules:
  - host: apps.mfe-demo.phd.opencraft.hosting
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: caddy
            port:
              number: 80
  tls:
  - hosts:
    - apps.mfe-demo.phd.opencraft.hosting
    secretName: mfe-host-tls